{
  "name": "sitedao-scripts",
  "version": "1.0.0",
  "description": "SiteDAO deployment and utility scripts",
  "main": "deploy.ts",
  "scripts": {
    "deploy": "ts-node deploy.ts",
    "deploy:testnet": "ts-node deploy.ts --network testnet",
    "deploy:devnet": "ts-node deploy.ts --network devnet"
  },
  "dependencies": {
    "@mysten/sui": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
